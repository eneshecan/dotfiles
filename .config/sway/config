# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!


# Variables

# Modifier
set $mod Mod4

# Colors
set $bg_color_dark #101010DD
set $bg_color_unfocus #303030DD
set $indicator_color #E95420DD
set $border_color #A0A0A0DD
set $border_color_transparent #00000000
set $text_color_light #E0E0E0FF
set $text_color_dark #808080FF

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1  "1"
set $ws2  "2"
set $ws3  "3"
set $ws4  "4"
set $ws5  "5"
set $ws6  "6"
set $ws7  "7"
set $ws8  "8"
set $ws9  "9"
set $ws10 "10"



# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:JetBrainsMono Nerd Font 10


# Window Settings
titlebar_padding 16 6
title_align center
default_border pixel 2
smart_borders on
gaps inner 8


# Opacity
set $opacity 0.9
for_window [class=".*"] opacity $opacity
for_window [app_id=".*"] opacity $opacity


# Wallpaper
output "*" bg ~/.config/sway/wallpaper.png fill


# Gtk Settings workaround
exec_always {
    gsettings set org.gnome.desktop.interface gtk-theme "Yaru-dark"
    gsettings set org.gnome.desktop.interface icon-theme "Papirus-Dark"
    gsettings set org.gnome.desktop.interface font-name "JetBrainsMono Nerd Font 10"
    gsettings set org.gnome.desktop.interface cursor-theme-name "Adwaita"
    gsettings set org.gnome.desktop.interface cursor-theme-size "16"
}


# Mouse
input type:touchpad {
    tap enabled
    natural_scroll enabled
    scroll_factor 0.8
}


# Keymap
input * {
    xkb_layout "us,de,tr"
    xkb_variant ""
    xkb_options "grp:alt_caps_toggle"
}
input <identifier> xkb_model "pc105"


# Autostart apps
exec --no-startup-id dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
exec --no-startup-id kanshi
exec --no-startup-id mako
exec --no-startup-id eww daemon
exec --no-startup-id blueman-applet  # Required to run blueman-manager
exec --no-startup-id firefox
exec --no-startup-id swaymsg 'workspace $ws2; exec kitty'
exec --no-startup-id wl-paste -t text --watch clipman store --no-persist
exec swayidle -w \
    timeout 900  'swaylock' \
    before-sleep 'playerctl pause' \
    before-sleep 'swaylock'


# Keybindings

# General
bindsym $mod+Shift+q kill
bindsym $mod+p exec grim -g "$(slurp -d)" - | wl-copy && notify-send -u low -i camera-photo "Screenshot copied to clipboard"
bindsym $mod+shift+x exec swaylock
bindsym $mod+shift+p exec ~/.config/eww/launch.sh

# Keyboard
bindsym XF86AudioRaiseVolume exec --no-startup-id ~/.config/sway/scripts/volume.sh inc
bindsym XF86AudioLowerVolume exec --no-startup-id ~/.config/sway/scripts/volume.sh dec
bindsym XF86AudioMute exec --no-startup-id ~/.config/sway/scripts/volume.sh mute
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous
bindsym XF86_MonBrightnessUp exec --no-startup-id ~/.config/sway/scripts/brightness.sh -A
bindsym XF86_MonBrightnessDown exec --no-startup-id ~/.config/sway/scripts/brightness.sh -U

# Commonly used apps
bindsym $mod+d exec wofi
bindsym $mod+Return exec kitty
bindsym $mod+i exec firefox
bindsym $mod+c exec clion
bindsym $mod+g exec gedit
bindsym $mod+t exec chromium --profile-directory=Default --app-id=ipfigjgcnonmflnplkgfnhmilbghenkp -enable-features=UseOzonePlatform -ozone-platform=wayland
bindsym $mod+m exec thunderbird
bindsym $mod+shift+s exec spotify

# Switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Move focused workspace between monitors
bindsym $mod+Ctrl+Right move workspace to output right
bindsym $mod+Ctrl+Left move workspace to output left

# Change focus
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move focused window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Split in horizontal orientation
bindsym $mod+h split h

# Split in vertical orientation
bindsym $mod+v split v

# Enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# Default workspace layout
workspace_layout default

# Change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# Focus the parent container
bindsym $mod+a focus parent

# Focus the child container
bindsym $mod+Shift+a focus child

# Reload the configuration file
bindsym $mod+Shift+c reload
# Restart sway inplace (preserves your layout/session, can be used to upgrade sway)
bindsym $mod+Shift+r restart

# Resize Mode (You can also use the mouse for that)
bindsym $mod+r mode "resize"

mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym Left resize shrink width 5 px or 5 ppt
    bindsym Down resize grow height 5 px or 5 ppt
    bindsym Up resize shrink height 5 px or 5 ppt
    bindsym Right resize grow width 5 px or 5 ppt

    # Back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}


# Assign windows to specific workspace
assign [app_id="firefox"] $ws1
assign [app_id="chromium"] $ws7
# assign [app_id="kitty"] $ws2
assign [class="jetbrains-clion"] $ws4
assign [app_id="gedit"] $ws4
assign [class="Microsoft Teams - Preview"] $ws7
assign [class="Signal"] $ws7
assign [app_id="glade"] $ws5
assign [class="VirtualBox Manager"] $ws5
for_window [class="VirtualBox Machine"] move to workspace $ws6
assign [app_id="thunderbird"] $ws8
for_window [class="Spotify"] move to workspace $ws9


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Floating Windows
for_window [app_id="pavucontrol"] floating enable
for_window [app_id="blueman-manager"] floating enable
for_window [title="nmtui"] floating enable
for_window [class="Nm-connection-editor"] floating enable
for_window [app_id="gtkgui.x86"] floating enable
for_window [app_id="channeldisplay_gui.x86"] floating enable
for_window [app_id="consoledisplay.x86"] floating enable
for_window [app_id="mx_updater.x86"] floating enable
for_window [app_id="panel_emu.x86"] floating enable
for_window [app_id="image_creator.x86"] floating enable
for_window [app_id="whatsapp-for-linux"] floating enable
for_window [title="xeyes"] floating enable


# Bar
bar {
    swaybar_command waybar
}


# Client side decorations
# class                 border                    background        text              indicator         child_border
client.focused          $border_color             $bg_color_dark    $text_color_light $indicator_color  $border_color
client.unfocused        $border_color_transparent $bg_color_unfocus $text_color_dark  $bg_color_unfocus $border_color_transparent
client.focused_inactive $border_color_transparent $bg_color_unfocus $text_color_dark  $bg_color_unfocus $border_color_transparent

